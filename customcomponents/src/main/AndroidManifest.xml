<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <application>
        <provider
            android:name="net.c306.customcomponents.utils.CustomComponentsFileProvider"
            android:authorities="${applicationId}.custom_components.email_file_provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths_customcomponents" />
        </provider>
    </application>
</manifest>
